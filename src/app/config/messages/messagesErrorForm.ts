import { IFormErrorMessages } from '@cmx-lib/validation';

export const messagesErrorForm: IFormErrorMessages = {
  required: 'Campo requerido..',
  email: 'correo no valido..',
  maxlength: 'Maxico de caracteres superado',
};
